#include<iostream>
using namespace std;
int main(){
    int arr[]={4,2,3,1,5,6};
   /* cout << arr[0];
    //cout << arr[6];
    cout << arr[1] << endl;
    cout <<"Elemant of array:";
    for(int i =0 ; i<6;i++){
        cout << arr[ i];
    }
    int sum=0;
    for(int i =0;i <6;i++){
        sum+=arr[i];
      
    }
     cout << "\nSum of Array:"<<sum<<endl;

    int average = sum/6;
    cout << "\n Average of Array:"<< average;
    int max = arr[0];
    for(int i=0;i<6;i++){
    if(max<arr[i]){
       max=arr[i];
    }
    }
    cout <<" \nMaximum number of an array:" << max;
    int min = arr[0];
    for(int i=0;i<6;i++){
    if(min>arr[i]){
       min=arr[i];
    }
    }*/
    for(int i=0;i<6;i++){
        if( % 2 = 0){
            cout << arr[i];
        }
    }
   // cout <<" \nMinimum number of an array:" << min;
    return 0;
}